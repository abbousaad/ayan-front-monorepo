# AT-16 — Add public orders route and nav link to admin panel

## Goal
Wire the new `AdminPublicOrdersPage` into the admin router and add a "Public Orders" link to the admin sidebar navigation.

## Requirements

### Router
- Import `AdminPublicOrdersPage` from `./routes/admin/admin-public-orders-page`
- Add a new child route under the `/admin` layout: `{ path: 'public-orders', element: <AdminPublicOrdersPage /> }`
- Place it after the existing `orders` route in the children array

### Navigation
- Add a new entry to the `NAV_ITEMS` array in `admin-layout.tsx`: `{ label: 'Public Orders', to: '/admin/public-orders' }`
- Place it after the existing "Orders" entry

## Key files to read
- `/Users/absaad/Desktop/ayan-front-monorepo-oac/apps/web/src/router.tsx` -- the router config where all admin routes are defined
- `/Users/absaad/Desktop/ayan-front-monorepo-oac/apps/web/src/admin/admin-layout.tsx` -- the sidebar layout with `NAV_ITEMS` array

## Files to edit
- `apps/web/src/router.tsx` -- add import and route
- `apps/web/src/admin/admin-layout.tsx` -- add nav item

## Constraints
- Keep the same ordering convention: new route goes right after the `orders` route
- Named import only (no default imports)
- Run `pnpm --filter web lint` before marking done

## Done when
- [ ] Navigating to `/admin/public-orders` renders the public orders page
- [ ] "Public Orders" link appears in the admin sidebar after "Orders"
- [ ] The NavLink highlights correctly when active
- [ ] `pnpm --filter web lint` passes
- [ ] Task marked `[x]` in `.claude/tasks/active.md`
