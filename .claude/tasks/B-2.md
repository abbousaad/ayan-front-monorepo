# B-2 — Make mobile product cards full width on home and store screens

## Goal

Change the mobile product listing layout so each product card spans the full available width on both the home screen and the store screen.

## Files to modify
- `apps/mobile/src/home-screen.tsx`
- `apps/mobile/src/store-products-screen.tsx`
- `apps/mobile/src/components/product-card.tsx`

## Fixes

### Home screen list
- Remove the 2-column product grid.
- Render products in a single-column list.

### Store screen list
- Remove the 2-column product grid.
- Render products in a single-column list.

### Product card footer layout
- Keep price aligned to the left.
- Keep add button or quantity selector aligned to the right.
- Ensure the card fills the row cleanly.

## Done when
- [x] Home screen product cards are full width
- [x] Store screen product cards are full width
- [x] Price is left-aligned and unit selector is right-aligned
- [x] `pnpm --filter mobile lint` passes
- [x] Task marked done in `.claude/tasks/activemobile.md`
